import { MettrePremierCharEnMajusPipe } from './mettre-premier-char-en-majus-pipe';

describe('MettrePremierCharEnMajusPipe', () => {
  it('create an instance', () => {
    const pipe = new MettrePremierCharEnMajusPipe();
    expect(pipe).toBeTruthy();
  });
});
